export default class Anwsers {
  /**
   * 
   * @param {{
   * id: string,
   * question_id: string,
   * anwser: string,
   * }} data 
   */
  static async create(data) {
    const res = await Promise.resolve({});
    return res;
  }
} 