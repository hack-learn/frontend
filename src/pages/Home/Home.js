import React from 'react';
// import { useContext } from 'react';
// import StoreContext from '../../store';
import ComputerDesk from '../../assets/img/female';

const Home = () => {
  // const { globalState, dispatch } = useContext(StoreContext);

  return (
    
    <ComputerDesk />  
    
  );
};

export default Home;
