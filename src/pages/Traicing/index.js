import React from 'react';
import { Link } from 'react-router-dom';
// import { useContext } from 'react';
// import StoreContext from '../../store';

const Traicing = () => {
  // const { globalState, dispatch } = useContext(StoreContext);
  return (
    
    <div>
        <table></table>
    </div>
    
  );
};

export default Traicing;